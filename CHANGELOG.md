# بسم الله الرحمن الرحيم

# Change log → [`docs/CHANGELOG.md`](docs/CHANGELOG.md)

The canonical change log lives inside `docs/` so the tree mirrors the operator's docs folder and `check-docs` guards its freshness. Newest entries first, there.
